<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZebraPrinterKCH">

        <!-- FileProvider for scan editing output -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Launcher / Home -->
        <activity
            android:name=".landing.LandingActivity"
            android:exported="true"
            android:theme="@style/Theme.ZebraPrinterKCH">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Scan flow -->
        <activity
            android:name=".scan.ScanDeliverySlipActivity"
            android:exported="false"
            android:theme="@style/Theme.ZebraPrinterKCH"
            android:parentActivityName=".landing.LandingActivity" />

        <activity
            android:name=".scan.ScanResultActivity"
            android:exported="false"
            android:theme="@style/Theme.ZebraPrinterKCH"
            android:parentActivityName=".landing.LandingActivity" />

        <!-- Other screens -->
        <activity android:name=".product.ProductsActivity" android:exported="false" android:theme="@style/Theme.ZebraPrinterKCH" />
        <activity android:name=".grn.ui.GrnActivity" android:exported="false" android:theme="@style/Theme.ZebraPrinterKCH" />
        <activity android:name=".ZPLPrinterActivity" android:exported="false" />
        <activity android:name=".product.lots.LotsListActivity" android:exported="false" />
    </application>
</manifest>
